## Add misc shortcuts and scripts to path
export PATH=$HOME/.local/share/JetBrains/Toolbox/scripts:$HOME/Projects/Exercism/scripts:$PATH

## Use lf to switch directories and bind it to ctrl-o
lfcd() {
  tmp="$(mktemp)"
  lf -last-dir-path="$tmp" "$@"
  if [ -f "$tmp" ]; then
    dir="$(cat "$tmp")"
    rm -f "$tmp"
    [ -d "$dir" ] && [ "$dir" != "$(pwd)" ] && cd "$dir"
  fi
}
bindkey -s '^o' 'lfcd\n'

## Bind ctrl + arrow right/left arrow keys to forward-word/backward word
bindkey "^[[1;5C" forward-word
bindkey "^[[1;5D" backward-word

## Use exa-wrapper.sh (renamed to els here) if possible
if command -v exa >/dev/null; then
    alias els='els.sh'
else
    alias els='/bin/ls $LS_OPTIONS'
fi

##
qcom() {
  git add . && git commit -m "automated quick commit"
}

## Add exercism scripts to path


